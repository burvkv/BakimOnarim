@* @(Html.DevExtreme().Chart()
    .ID("chart")
    .Rotated(true)
    .DataSource("https://js.devexpress.com/Demos/WidgetsGallery/JSDemos/data/simpleJSON.json")
    .Series(s => s.Add()
        .Label(l => l
            .Visible(true)
            .BackgroundColor("#c18e92")
        )
        .Color("#79cac4")
        .Type(SeriesType.Bar)
        .ArgumentField("day")
        .ValueField("sales")
    )
    .Title("Yapılan İşler")
    .ArgumentAxis(a => a
        .Label(l => l
            .CustomizeText(@<text>
                function() {
                    return "Day " + this.valueText;
                }
            </text>)
        )
    )
    .ValueAxis(a => a.Add()
        .Tick(t => t.Visible(false))
        .Label(l => l.Visible(false))
    )
    .Export(e => e.Enabled(true))
    .Legend(l => l.Visible(false))
) *@



@(Html.DevExtreme().Chart()
    .ID("chart2")
    .Series(s => s
        .Add()
        .ArgumentField("Day")
        .ValueField("Oranges")
        .Name("Bitirilen İşler")
        .Type(SeriesType.Bar)
        .Color("#ffaa66")
    )
    .DataSource(new[] {
        new { Day = "Pazartesi", Oranges = 3 },
        new { Day = "Salı", Oranges = 2 },
        new { Day = "Çarşamba", Oranges = 3 },
        new { Day = "Perşembe", Oranges = 4 },
        new { Day = "Cuma", Oranges = 6 },
        new { Day = "Cumartesi", Oranges = 11 }
    })
)
